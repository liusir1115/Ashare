# Tushare 10000 积分接入准备清单

本文档用于确认：当项目切换到 `Tushare Pro` 主数据源后，是否已经具备“接入后即可开始测试盘前筛选”的条件。

## 已确认完成

- `tushare` 依赖已安装
- 后端入口可正常导入
- 盘前主筛选已经切到 `Tushare`
- 本机已经实测通过自定义入口：
  - `pro.index_basic(limit=5)`
  - `ts.pro_bar(api=pro, ts_code="000001.SZ", limit=3)`
- 当前初始化方式已固定到：
  - `akshare_backend/tushare_runtime_local.py`

## 当前初始化口径

后续统一使用：

- token
- `pro._DataApi__http_url = "http://124.220.22.110:8020/"`

也就是说，项目不再依赖“每次开终端手动配置环境变量”这种方式。

## 当前还能做什么

- 立刻测试盘前主筛选链路
- 立刻测试 `stock_basic` / `daily` / `daily_basic` / `pro_bar`
- 保留现有 AKShare 新闻摘要辅助链路

## 当前还未完成的事

- 将更多高积分字段继续映射进正式筛选项
- 继续补强资金流、筹码、题材映射等增强维度
- 将同一套数据链路进一步接到盘后复盘模式

## 当前结论

如果你的目标是“把数据接进来就开始测试筛选效果”，那么这台机器现在已经具备条件，可以继续联调，不需要再额外折腾初始化方式。
