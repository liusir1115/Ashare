# Archive Folder

This folder stores helper material that is not part of the current active product flow
but may still be useful for future reference.

Rules:

- Do not place runtime garbage here
- Delete logs, caches, and temporary output instead of archiving them
- Move dormant helper scripts here only when they are not part of the active execution path
