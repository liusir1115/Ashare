# Backend Helper Archive

This directory contains older validation helpers that are not part of the current
main backend flow.

If one of these helpers becomes needed again, move it back into an active `scripts`,
`tests`, or backend support location with a clear reason.
